{
  "type": "object",
  "properties": {
    "CALENDAR": {
      "type": "string",
      "enum": ["sport", "shopping", "home", "working"]
    },
    "TIMESTAMP": {
      "type": "string"
    },
    "UUID": {
      "type": "string"
    }
  },
  "required": ["CALENDAR", "TIMESTAMP", "UUID"]
}
{
  "type": "object",
  "properties": {
    "LABELS": {
      "type": "string",
      "enum": ["left", "right", "move", "stop"]
    },
    "TIMESTAMP": {
      "type": "string"
    },
    "UUID": {
      "type": "string"
    }
  },
  "required": ["LABELS", "TIMESTAMP", "UUID"]
}